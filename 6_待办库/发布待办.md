# 发布待办（GitHub + Vercel）

> 状态：暂停，明天继续 | 记录于 2026-06-10

---

## 已确认的发布决策（用户 2026-06-10 拍板）
- 上传范围：**整个项目**（含知识库）
- 仓库可见性：**公开 public**
- 建仓方式：**帮装 gh CLI 自动建**（brew 安装 → 登录 → 创建并推送）

⚠️ 提醒：公开整个项目=知识库(策略/仓位/复盘 IP)全部公开。明天动手前**再向用户确认一次**是否仍接受，可考虑改私有或仅传 platform/。

---

## 待办清单
- [ ] 用户说"还有一些东西错了" → **先修错误,再发布**（明天问清楚是哪些）
- [ ] 装 gh CLI（`brew install gh`）+ `gh auth login`
- [ ] 写 `.gitignore`（排除 `__pycache__/`、`*.pyc`、本地日志、临时文件；DB 是否纳入见下）
- [ ] `git init` + 首次提交
- [ ] 创建公开仓库并推送
- [ ] Vercel 部署适配（**关键架构问题**）：
  - Vercel 无服务器=只读临时文件系统，SQLite 写不了
  - 方案：只读部署——`wcp.db` 打包进仓库，线上只读；数据更新靠重新部署
  - 需加 `api/index.py`(ASGI 入口)、`requirements.txt`、`vercel.json`
  - 确认 FastAPI 在 Vercel Python runtime 跑通
- [ ] 部署后验证三榜/进度/分类/邀请码链接线上正常

---

## 今日已完成（M1+M2 + 6项优化）
- 平台 `platform/wcp/`：数据管道→三榜引擎→FastAPI→前端
- 6 项优化全部完成（见 [7_优化库/用户优化要求.md](../7_优化库/用户优化要求.md)）
- `.claude/launch.json` 已配置(wcp-api:8731)
- DB 已抓取 497 个选题,邀请码统一 `?via=serene77mc-g6kj`
