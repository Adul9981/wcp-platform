# 平台总体规划

> 定位（已对齐 2026-06-10）：**公开付费产品**，只读+决策辅助，跳转 Polymarket 带邀请码，订阅付费后置。
> 建设顺序：MVP 先跑通**数据管道**。
> 受 [平台建设原则](平台建设原则.md) 约束。

---

## 一、产品一句话
"每天告诉你：世界杯预测市场里，最值得下注、最被忽视、盈亏比最高的机会在哪——并解释为什么。"

用户在我们这看决策，点链接（带我们邀请码）去 Polymarket 自己下单。

---

## 二、技术栈（已定）

| 层 | 选型 | 理由 |
|----|------|------|
| 抓取/分析 | **Python** | 已验证可用，数据生态最强 |
| 后端 API | **FastAPI** | 轻量、异步、自带文档，平滑支撑未来多用户 |
| 存储 | **SQLite → PostgreSQL** | MVP 用 SQLite 零运维；多用户阶段迁 Postgres |
| 定时任务 | **APScheduler / cron** | 每日快照、diff、三榜生成 |
| 前端 | **Next.js (React)** | 公开产品需要真前端；SEO 友好利于获客 |
| 部署 | 单机 Docker → 云 | MVP 单机，后续容器化上云 |

> MVP 阶段前端可先用极简页面甚至先不做，重点是数据管道 + API 跑通。

---

## 三、系统架构（分层，读写分离）

```
┌─────────────────────────────────────────────┐
│  前端 Next.js  (三榜 / 选题浏览 / 详情跳转)      │
└───────────────┬─────────────────────────────┘
                │ REST
┌───────────────▼─────────────────────────────┐
│  FastAPI 服务层                                │
│  - /boards 三榜   - /topics 选题   - /matches  │
│  - 用户/订阅(后置)                              │
└───────────────┬─────────────────────────────┘
                │
┌───────────────▼─────────────────────────────┐
│  数据层 (DB: 选题/快照/场次/策略标签/复盘)        │
└───────────────▲─────────────────────────────┘
                │ 写入
┌───────────────┴─────────────────────────────┐
│  数据管道 (Python, 定时)                        │
│  抓取 → 归一去重 → 策略打标 → diff → 三榜计算     │
└───────────────▲─────────────────────────────┘
                │ 只读抓取(无私钥)
        Polymarket Gamma/CLOB/Data API

  ※ 交易系统(纸面/真实)完全独立，MVP 不建；跳转用户自理
```

---

## 四、核心数据流（MVP 重点）

```
1. 抓取   tag 102350(长盘) + series 11433(单场)  [带UA/翻页/重试]
2. 归一   单场 348 event → 104 场次；统一成 Topic 模型
3. 打标   每个 Topic 自动匹配 strategy_tags [A..N]
4. 快照   存当前 price/volume/liquidity + snapshot_ts
5. diff   对比昨日 → 标 is_new(先发机会)
6. 估值   公平价值（MVP: 跨市场对比/简单规则；后期模型）
7. 三榜   按评分生成 最值得关注/最被忽视/最大盈亏比
8. 输出   写库 → API → 前端 / 文案系统
```

---

## 五、三榜定义（产品第一屏，恒在）

| 榜 | 入选逻辑 | 排序 |
|----|---------|------|
| 🎯 最值得关注 | 策略匹配度高 × 流动性足 × 赔率有优势 × 评分≥阈值 | 评分降序 |
| 💎 最被忽视 | 低关注/新上线 × 有策略逻辑 × 赔率未充分定价 | 信息差降序 |
| 📈 最大盈亏比 | 赔率高(隐含概率低) × 有逻辑支撑(非纯随机) | 盈亏比降序 |

每个条目必含：选题、方向、当前概率、公平估值、命中策略、**带邀请码的跳转链接**、数据时间。

---

## 六、建设里程碑

| 里程碑 | 内容 | 依赖 |
|--------|------|------|
| **M1 数据管道**（MVP核心） | 抓取→归一→打标→快照→diff，稳定落库 | 无 |
| **M2 三榜引擎** | 估值+评分+三榜生成，API 暴露 | M1 |
| **M3 前端展示** | 第一屏三榜 + 选题浏览 + 详情跳转(邀请码) | M2 |
| **M4 文案+复盘自动化** | 赛前/赛后/盘中文案，复盘记录 | M2 |
| **M5 订阅付费** | 账号、付费墙、权限 | M3 |
| **M6 交易(可选/后)** | 纸面→真实，强风控 | 独立 |

---

## 七、当前推进位置
- ✅ 选题盘点、数据源核查、原则、待办/优化库
- ▶️ **下一步：细化 M1 数据管道的待办，开始建设**

详见 [6_待办库](../6_待办库/)
